{
  "name": "fan-friction-radar",
  "private": true,
  "scripts": {
    "typecheck": "pnpm -r --filter @ffr/shared --filter @ffr/api --filter @ffr/ingest exec tsc --noEmit",
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "pnpm typecheck && pnpm test",
    "lint": "eslint packages/",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "22.15.17",
    "eslint": "9.27.0",
    "prettier": "3.5.3",
    "tsup": "8.4.0",
    "typescript": "5.8.3",
    "vitest": "3.1.3",
    "fast-check": "4.1.1"
  }
}
